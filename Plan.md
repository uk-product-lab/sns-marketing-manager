# SNSマーケティング管理サービス Plan

更新日: 2026-09-16（最終構想・現MVP・未決の基盤選定を分離。過去の料金比較は採用判断に使わず、実接続前に再確認する）

## 目的

複数ブランドで運用するInstagram、X、TikTok、noteなどの投稿を、Mac mini、MacBook、iPhoneのどこからでも確認・準備・予約・再利用できる、自分用のレスポンシブWebサービスを作る。

初期版は個人利用を優先するが、将来Webサービスとして複数利用者へ展開できるデータ構造にしておく。

最終ゴールは、長編動画、録音音声、写真、文章を受け取り、専門担当が編集して、共通のマーケティング企画から短尺動画・画像スライド・SNS原稿・note記事と販売先URL・LPへ展開できること。入力素材の長さと出力する短尺動画の長さは別の制約として扱う。

これは製品の最終要件であり、すべてを初期MVPへ前倒しする決定ではない。今回承認された作業は文書・ハーネス整備まで（DEC-010）。制作基盤やAPI利用の実接続、LP公開は別工程とする。

## 決定済みの方針

- MVPでは課金が発生する外部サービスを使わない。無料枠を超えた場合に自動課金される構成にも移行しない。
- サービス本体はCloudflareを中心に構成する。
- データベースはCloudflare D1、アプリとAPIはCloudflare Workersを基本方針とする。画像・動画の既存候補はR2だが、追加課金禁止を満たす条件が確認できるまで実接続しない。非公開Driveとの分担・変更はDEC-011で決める。
- Google Workspaceは未契約、Google AI Proは契約済み。専用Driveフォルダ利用は候補にできるが、容量・残量・API権限・開発者特典は未確認。今回フォルダは作成しない。Drive、Docs、Sheets連携をMVPの無条件な前提にはしない。
- 画面はレスポンシブ対応し、iPhoneではホーム画面から使えるPWAを目指す。
- SNS APIによる自動投稿はMVPに含めない。すべての媒体について、生成・編集・コピー・素材取得までをサービス内で行い、実際の投稿はiPhoneまたはMacBookから手動で行う。
- 投稿状態は `下書き / 編集中 / 投稿準備完了 / 投稿予定 / 要手動 / 投稿済み / 見送り` を基本とする。
- 「投稿予定」はサービス内の予定であり、SNS側の予約投稿を意味しない。予定時刻になった投稿は「要手動」として表示する。
- 生成物、過去投稿、下書き、画像、動画を後から検索・複製・再利用できることをMVPの中心要件にする。
- 個人利用時も、すべての主要データに利用者・ブランドの所属情報を持たせ、将来の複数利用者対応に備える。
- 認証にはBetter Authを使い、Google認証とメールアドレス・パスワード認証を用意する。

## MVPの5機能

安定した要件IDをIssue・PR・テスト・証跡へ引き継ぐ。以下の詳細が正本であり、IDだけで要件を縮小しない。

| 要件ID | 範囲 | 現在の段階 |
|---|---|---|
| REQ-MVP-01 | ブランド・アカウント管理 | MVP |
| REQ-MVP-02 | 企画・媒体別原稿・履歴・コピー | MVP |
| REQ-MVP-03 | 素材検索・複製・派生関係・画像最大10枚 | MVP |
| REQ-MVP-04 | JSTカレンダー・予定・要手動表示 | MVP |
| REQ-MVP-05 | 手動投稿パック・取得・投稿URL記録 | MVP |
| REQ-AUTH-01 | Better Auth、Google/メール+パスワード、利用者制限 | MVP |
| REQ-MOBILE-01 | Mac/iPhoneレスポンシブ、コピー・取得 | MVP、実機検証は公開前 |
| REQ-MEDIA-01 | 15〜30秒9:16出力、字幕編集・SRT/WebVTT | MVP、焼き込みは次期 |
| REQ-FUTURE-01 | 投稿成果・アクセス解析 | 必ず保持 |
| REQ-FUTURE-02 | AI文章生成・履歴・ブランド表現 | 必ず保持 |
| REQ-FUTURE-03 | 長尺入力・動画/音声/写真の編集 | 必ず保持 |
| REQ-FUTURE-04 | 統合企画からnote+販売URL・LP生成 | 必ず保持 |
| REQ-FUTURE-05 | Google連携・将来の複数利用者サービス化 | 条件確認後 |

共通制約: 追加課金なし、SNS自動投稿なし、原本上書きなし、独立レビュー・必須テスト・本人承認を維持する。

### 1. ブランド・運用アカウント管理

- 複数ブランドを登録できる。
- 各ブランドにInstagram、X、TikTok、noteなどの運用アカウントを紐づける。
- 媒体ごとにアカウント名、プロフィールURL、投稿画面へのリンク、手動投稿時の注意事項を管理する。

### 2. 投稿企画と媒体別原稿

- 1つの投稿企画に、共通テーマ、目的、素材、公開希望日時を持たせる。
- 同じ企画からInstagram用、X用、TikTok用、note用の原稿を個別に管理する。
- 媒体別の文字数、画像枚数、動画形式などを確認できる。
- 原稿の変更履歴を残し、過去版へ戻せるようにする。
- 投稿キャプションは、本文、ハッシュタグ、リンク、代替テキスト、メモを分けて編集できる。
- 投稿時には「すべてコピー」「本文だけコピー」「ハッシュタグだけコピー」を使い分けられる。
- iPhoneでコピーしやすいように、投稿直前画面では編集機能よりも大きなコピーボタンと確認表示を優先する。

### 3. コンテンツ・素材ライブラリ

- 生成した文章、過去投稿、下書き、画像スライド、9:16動画を一か所に保存する。
- ブランド、媒体、投稿形式、状態、タグ、作成日、公開日で絞り込める。
- キーワード検索、複製、新しい投稿企画への再利用ができる。
- 素材にはファイルサイズ、縦横寸法、再生時間、形式、作成元、バージョンを記録する。
- 元素材と、切り抜き・字幕入り・圧縮版などの派生素材を関連づけられる構造にする。
- 画像スライドは1投稿につき最大10枚とする。
- 画像はドラッグまたはボタンで並べ替え、差し替え、削除ができる。
- 保存時は `01` から `10` の連番を付け、iPhoneやMacBookへ取得した後も順番が分かるようにする。

### 4. 投稿カレンダーと予約

- 月、週、一覧で投稿予定を確認する。
- ブランドと媒体で絞り込める。
- 日本時間を基準に公開予定を登録し、媒体別に予定時刻を変更できる。
- 同じ時間帯の重複投稿や、未完成原稿を警告する。
- 予定時刻になるとアプリ内で「要手動」に切り替える。SNS APIへの送信処理は行わない。

### 5. 手動投稿パックと状態管理

- 媒体ごとに `投稿準備完了 / 投稿予定 / 要手動 / 投稿済み / 見送り` を表示する。
- 投稿直前画面に、投稿キャプション、画像または動画、投稿順、確認事項をまとめる。
- 本文・ハッシュタグのコピー、個別素材の保存、画像一括取得、端末の共有メニューを使えるようにする。
- 画像一括取得時は連番ファイル名を保持する。
- 手動投稿後に「投稿済み」へ変更し、投稿先URLと実際の公開日時を記録できる。
- 媒体ごとの手動投稿チェックリストを表示する。

## 媒体別の初期対応

| 媒体 | MVPでの扱い | 補足 |
|---|---|---|
| Instagram | 要手動 | キャプションコピー、画像・動画取得、投稿済み記録を提供する |
| X | 要手動 | 有料APIは使わず、キャプションコピーと素材取得までを提供する |
| TikTok | 要手動 | キャプションコピー、動画取得、投稿済み記録を提供する |
| note | 要手動 | 原稿コピー、画像取得、投稿チェックリストを提供する |

## 推奨アーキテクチャ

- フロントエンド: React + TypeScript + Vite
- 実行環境・API: Cloudflare Workers
- データベース: Cloudflare D1
- 画像・動画: 保存先は未決。Cloudflare R2、非公開Google Drive、Mac miniの役割をDEC-011で決める
- 認証: Better Auth
- Better Authの保存先: D1 + Drizzle ORMのSQLiteアダプター
- 認証方式: Google OAuth、メールアドレス + パスワード
- 任意の次期連携: Google Drive、Docs、Sheetsからの素材・原稿取り込み。Google Workspace契約は前提にしない。

予約日時はD1へ保存する。SNSへのバックグラウンド送信がないため、MVPではCron TriggersとQueuesを使わない。画面を開いた際に現在時刻と予定時刻を比較し、「要手動」を表示する。

Google OAuthのClient SecretとBetter Authの秘密鍵はWorkersの秘密情報として保存し、コードやD1へ直接書き込まない。

### Better Authの初期認証方針

- 通常のログインはGoogle認証を推奨する。
- メールアドレス認証は、MVPでは「メールアドレス + パスワード」とする。
- メール送信サービスを追加しないため、MVPではマジックリンク、確認メール、パスワード再設定メールを必須にしない。
- 個人利用中は登録可能なメールアドレスを制限し、第三者が自由に新規登録できないようにする。
- Google認証と同じメールアドレスのメール・パスワードアカウントは、同一利用者として連携できる構成にする。
- 将来一般公開する前に、メール確認、パスワード再設定、不正ログイン対策を追加する。

## 独自ドメインの判断

課金しない期間は独自ドメインを購入せず、Cloudflareの `workers.dev` ドメインを利用する。Google OAuthのコールバックURLも、この固定URLを登録する。

将来有料サービスを利用可能にした場合の想定構成:

- `app.example.com`: 管理画面
- `media.example.com`: SNSが取得する公開用画像・動画

R2は公開バケットにせず、ログイン済み利用者だけが短時間利用できる署名付きURLで素材を取得する。SNS APIへメディアURLを渡さないため、本番用の公開メディアドメインはMVPでは不要。

## 動画ストレージの判断（実接続前に再確定）

R2は既存の保存先候補。原本を既存契約内の非公開Driveへ置く案と比較し、DEC-011で再確定する。Cloudflare Streamはまだ導入しない。無料枠・操作回数・転送・自動課金条件は、採用判断時に公式情報と実アカウント画面で再確認する。

理由:

- R2はWebアプリから完成物を扱う候補だが、追加課金禁止を技術的・運用的に満たせるか未確認である。
- 非公開Driveは既存契約内の原本庫候補だが、アプリからのAPI利用条件・権限・無人処理能力は未確認である。
- Streamは自動エンコード、複数画質、Web上での快適な再生が必要になった段階で検討すればよい。

目安:

- 基本動画は15〜30秒の9:16ショート動画とする。
- H.264、5〜10Mbps程度で書き出す場合、1本は概ね10〜40MBを想定する。
- 実素材の平均サイズ、原本と派生物の保持期間、取得回数を代表サンプルで測定してから容量上限を決める。

MVPからファイルサイズと利用量を記録する。R2を選ぶ場合の7GB・9GB警告は補助策であり、操作回数の超過や自動課金の防止を保証しない。原本を自動削除せず、削除やアーカイブは利用者が明示的に判断する。

### 投稿キャプションと動画内字幕

「キャプション」という言葉を、以下の2種類に分けて扱う。

1. 投稿キャプション: SNSの投稿欄へ貼り付ける本文・ハッシュタグ。MVPで編集、媒体別保存、コピーに対応する。
2. 動画内字幕: 動画上に表示する時間付きテキスト。MVPでは字幕行、開始・終了時刻、文字、位置、基本スタイルを編集し、動画と重ねてプレビューできるようにする。

動画内字幕はSRTまたはWebVTTとして保存・取得できるようにする。字幕を動画へ焼き込んだ新しいMP4の生成は、サーバー課金を避けるため、将来MacBookのブラウザ内で処理する方式を優先検討する。iPhoneでは字幕の確認と軽微な修正を優先し、重い動画書き出しを必須にしない。

## X API料金の扱い

「月500投稿の無料枠」は過去のプラン情報として扱い、現在利用できるとは仮定しない。料金・無料枠・既存アカウントの移行条件は変わり得るため、将来API連携を再検討する時点の公式案内とDeveloper Consoleを正本にする。

現在の決定は料金の多寡にかかわらずX APIを使わず、手動投稿パックまでで止めること。利用者が課金を明示承認しない限り、API契約やクレジット購入へ進まない。

## Googleサービスの任意連携

Google Workspaceは未契約、Google AI Proは契約済み。追加契約を求めず、既存契約の適用範囲を確認してから検討する。AIアプリの利用枠と独自WebからのAPI課金・無人実行は同一視しない。

- Google Docs: 長文原稿、note原稿、レビュー
- Google Sheets: 投稿企画表や既存管理表の取り込み
- Google Drive: 既存素材の参照・取り込み
- Google Calendar: 必要になれば公開予定の閲覧用同期

SNS自動投稿やSNSトークン管理には使わない。Driveを動画原本・完成物の保存先にする案はDEC-011で比較する。専用フォルダを置くことと、アプリのアクセス権限を限定することは別であり、作成/明示選択したファイルに絞る方式を優先する。

## 必ず保持する次期タスク

以下はMVP完了後に削除せず、優先順位を再評価して実装する。

### 投稿成果・アクセス解析

- 投稿URLと媒体側IDの保存
- 表示、再生、いいね、コメント、保存、クリックなどの取得
- ブランド・媒体・投稿形式別の比較
- 週次・月次レポート
- APIで取得できない指標の手入力

### AI文章生成

- 共通テーマから媒体別原稿を生成
- 既存原稿の短縮、言い換え、トーン変更
- ブランドごとの文章ルール
- 生成前プロンプト、生成結果、編集後原稿の履歴
- AI生成物もコンテンツ・ライブラリから検索・再利用

### 動画編集

- 9:16プレビュー
- 時間付き字幕の編集とプレビューはMVPへ前倒し
- 字幕のSRT・WebVTT取得はMVPへ前倒し
- トリミング、尺調整、表紙選択
- 字幕・テロップを焼き込んだMP4の端末内生成
- 音量と無音区間の確認
- 媒体別の書き出し
- Cloudflare Stream、Media Transformations、または外部動画処理基盤の比較

### Googleサービス連携（任意）

- Docsから投稿原稿を取り込む
- Driveから素材を選択する
- Sheetsの既存投稿表を移行する
- 必要に応じてGoogle Calendarへ閲覧用予定を同期する

### 統合メディア制作

- 長編動画、録音、写真、文章を企画へ関連付ける。受入上限は代表素材と処理性能を確認して決める。
- 動画切り抜き、音声編集、写真編集、文章編集を専門担当へ分け、編集長が目的・根拠・訴求・CTAを統一する。
- 利用者が指定した販売URLをSNS・note・LPへ一貫して反映し、未確認の価格・実績・特典を作らない。
- 原本、文字起こし、編集レシピ、成果物の版、承認、投稿記録を追跡する。
- 制作状態と投稿状態を分ける。公開承認は特定バージョンに結び付ける。
- Mac miniで重い処理、Web/iPhoneで依頼・確認する案を検証する。端末停止中は制作待ちを表示し、再試行・中断・重複防止を設計する。
- LPは生成・プレビューと外部公開を分ける。SNS・note投稿は手動を維持する。
- 具体的な担当、情報の正本、Notion運用は `harness/project_operations.md` を参照する。

### 将来のWebサービス化

- Better Authを使った一般利用者向け登録
- メール確認、パスワード再設定メール、二要素認証
- 複数利用者と権限
- 承認フロー
- 利用プランと課金
- 監査ログ
- Meta・TikTokなどの本番アプリ審査

## 代替サービスの比較結果

この比較は構成上の候補整理であり、現在の料金・無料枠・利用規約の確認結果ではない。実接続前に公式情報と実アカウント画面で再検証する。

### Cloudflare

WorkersとD1を本体の第一候補とする。R2とQueuesは一括採用せず、今回の用途・追加課金禁止・DEC-011に照らして個別に判断する。

### Supabase

PostgreSQLと認証機能を一体で扱える候補。認証やD1運用に重大な問題が出た場合の比較対象とし、保存容量・休止条件・課金条件はその時点で確認する。

### Firebase

Googleアカウントとの親和性はあるが、Google AI ProやGoogle Workspaceの契約とFirebaseの開発・従量課金条件を同一視しない。採用する場合は別途確認する。

### Vercel

画面公開の候補だが、DB・動画保存・バックグラウンド処理との組み合わせが必要になる。プラン条件と将来サービス化の可否を採用時に確認する。

### 結論

Cloudflareを本体の第一候補とし、追加課金を防ぐ運用条件を実装前に確認する。Google Workspace契約には依存しない。認証やDB操作で大きな開発上の問題が判明した場合のみ、Supabaseとのハイブリッド構成を再検討する。

## 実装前に確認する運用情報

- 現在運用しているブランド数
- ブランドごとの媒体とアカウント数
- 1週間あたりの投稿本数
- 1本の投稿を作る実際の手順
- 15〜30秒動画の実際の平均ファイルサイズと書き出し設定
- 既存素材の保存場所（端末、Google Driveなど）
- Instagramアカウントがプロアカウントになっているか

## 次の作業

1. 文書・ハーネス整備（TASK-0001）と独立レビュー。文書・ローカル検証器だけをdraft PRへ提示し、mergeは別途判断する。
2. 別タスクでVite starterへ実体のある型検査、Lint、単体、E2E、アクセシビリティ、smokeの基盤を追加する。空scriptや常時成功scriptで代用しない。
3. 品質scriptが動くことを確認してからworkflowを別governance PRで導入し、新方針の実GitHub準備ゲートで本人承認の正の試験・不正を拒否する負の試験を実施する。
4. `REPOSITORY_GATE_PASS`後、最初の縦切り「1ブランドの原稿保存→スマホで確認・コピー→投稿済み記録」を実装する。合格後、5機能と字幕要件を順次満たす。
5. 原本1件から短尺1本・媒体別原稿を作る制作実証。初期は手動連携を許容し、実行能力を証明してから自動化する。
6. 画像・音声・note・LPの統合制作、解析、AI文章生成、将来サービス化へ拡張する。

途中の縦切り合格をMVP全体の完了にしない。全要件とテスト証跡の対応を確認し、`Harness.md`に従って独立ブラウザE2Eゲートを通過した変更だけを次工程へ進める。
