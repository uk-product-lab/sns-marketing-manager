{
  "name": "codex-new-project",
  "version": "0.1.0",
  "private": true,
  "description": "A small starter web project created with Codex.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
